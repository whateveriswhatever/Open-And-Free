/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
  },
};

export default config;

// postcss.config.js;
// module.exports = {
//   plugins: ["tailwindcss", "autoprefixer"],
// };
